{
  "name": "the-way-back-home",
  "description": "Instructs your users how to install your website as a PWA.",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap --no-ci",
    "build": "lerna run build",
    "dev": "cd packages/estimtest-core && npm run build",
    "test": "lerna run test"
  },
  "workspaces": [
    "packages/*"
  ],
  "author": "KyleSmith0905",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
		"lerna": "^6.5.1",
    "typescript": "^4.9.5"
  }
}